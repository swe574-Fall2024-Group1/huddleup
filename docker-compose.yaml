version: '3'

services:
  backend:
    build:
      context: ./huddleupAPI
    ports:
      - "8000:8000"
    volumes:
      - ./huddleupAPI:/code
  frontend:
    build:
      context: ./huddleupui
    ports:
      - "80:3000"
    volumes:
      - ./huddleupui:/app
    depends_on:
      - backend